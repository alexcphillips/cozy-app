import styles from "./Profile.module.css";

/**
 * Placeholder. The file existed but was empty; it is routed now so the
 * ProfileButton in the navbar has somewhere to go.
 */
export default function Profile() {
    return <div className={styles["page-container"]}>profile</div>;
}
