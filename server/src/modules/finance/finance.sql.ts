/**
 * TODO: SQL for the finance module - budget, transactions, milestones, debts,
 * and the cash-flow aggregate. Statements as named constants, values always
 * through `$1`-style params. See `modules/diet/diet.sql.ts` for the pattern.
 */
